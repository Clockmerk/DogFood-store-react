import styles from "./footer.module.css";

export const Footer = () => {
  return (
    <footer className={styles.footer_section}>
      <section>
        <div>Контакты</div>
      </section>
    </footer>
  );
};
